# [CTF BugKu] /.-

一、题目信息
 
题目名称：/.
题目分类：Web
考点：目录遍历、URL路径构造、在线网站解码
 
二、题目描述
 
进入靶场初始页面，页面无输入框，提示和路径 /. 相关。直接访问原始页面看不到flag，访问根目录后获取到一串编码字符串，需要使用在线解码网站解密，才能得到最终flag。
 
三、解题思路
 
先构造路径访问服务器根目录，拿到编码后的密文；再打开在线解码网站，将密文粘贴进去解码，解密成功得到flag。全程纯手动操作，不使用代码。
 
四、全部手动解题方法
 
方法1：地址栏修改URL获取密文 + 在线网站解码（本题目标方法）
 
1. 打开靶场原始页面，点击浏览器地址栏。
2. 在URL末尾手动添加 /. ，按下回车访问服务器根目录。
3. 页面加载出根目录内容，找到一串编码后的密文，鼠标选中复制密文。
4. 新建浏览器标签页，打开在线解码网站。
5. 将复制好的密文粘贴到网站输入框，选择对应的解码类型，点击解码按钮。
6. 网站输出解密后的内容，读取得到flag。
 
方法2：开发者工具Network面板获取密文 + 在线网站解码
 
1. 打开靶场页面，快捷键 Ctrl+Shift+I 打开开发者工具，切换到Network网络面板。
2. 右键页面请求，选择Edit and resend编辑并重发请求。
3. 修改请求URL，路径末尾加上 /. ，点击Send发送请求。
4. 在Response响应内容中复制编码密文。
5. 打开在线解码网站，粘贴密文进行解码，拿到flag。
 
五、Flag
 
 flag{xxxxxxxxxxxx} 
 
六、题目总结
 
本题属于Web目录遍历入门题。利用目录遍历漏洞访问服务器根目录获取密文，再借助在线解码网站解密字符串，最终获取flag。
 

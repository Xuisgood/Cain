# CTF BugKu头等舱
题目名称：头等舱
 
题目分类：Web
考点：HTTP响应头、抓包分析
 
题目描述
 
访问靶场页面，页面空白，什么内容都不显示。查看网页源代码，里面也找不到flag。提示“头等舱”，暗示和HTTP头部（header）有关。
 
解题思路
 
网页的内容不只有页面展示的文字。浏览器和服务器通信时，服务器会返回HTTP响应头，响应头里面可以存放额外信息。
这道题把flag藏在了HTTP响应头中，页面主体空白，源码也没有，需要抓包查看响应头获取flag。
 
解题步骤（两种方法，优先浏览器，不用装Burp）
 
方法一：浏览器开发者工具（推荐新手）
 
1. 打开靶场网页。
2. 按  F12  打开开发者工具，切换到 Network（网络） 面板。
3. 按F5刷新页面，列表会出现hd.php这条请求，点击它。
4. 切换到 Response Headers（响应标头）。
5. 向下滚动响应头列表，找到一行自定义头部，里面就是flag。
 
方法二：Burp Suite抓包
 
1. 打开Burp，开启代理，浏览器配置代理。
2. 访问靶场链接，Burp抓到GET请求。
3. 点击Forward转发请求，查看返回的Response数据包。
4. 在响应头Headers区域找到flag。
 
原理分析
 
HTTP通信分为请求头（Request Headers）和响应头（Response Headers）。服务器返回数据时，除了返回网页HTML主体内容，还会附带响应头，用来传递一些附加信息。
本题服务器将flag写在自定义响应头中，页面主体故意留空，肉眼看不到。网页源代码（Ctrl+U）只能看到HTML主体，看不到响应头，所以必须通过网络面板或者抓包工具查看。
 
最终Flag
 
 flag{Bugku_k8_23s_istra} 
 
题目总结
 
看到题目名字提示“头、头等舱”这类关键词，优先想到HTTP Header。
页面空白、源码无内容时，不要只看页面，去网络面板查看响应头，这是Web入门常考题型。

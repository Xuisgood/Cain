# [CTF BugKu]POST
 
一、题目介绍
 
题目：POST
题型：Web基础
考察知识点：HTTP GET/POST 请求区别、手动构造 POST 数据包、开发者工具抓包修改请求方式
 
二、题目分析
 
进入靶场页面后，页面提示需要使用 POST 方式提交参数 what=flag 才能获取 flag。
 
普通在 URL 拼接参数属于 GET 请求，无法通关。
本题核心：必须手动把浏览器的 GET 请求改成 POST 请求，并传入指定参数。
 
三、解题思路
 
1. 普通访问网页是 GET 请求，服务器不返回 flag。
2. 需要手动抓包，将请求方法从 GET 修改为 POST。
3. 在请求体内手动添加参数  what=flag 。
4. 放行数据包，服务器返回 flag。
 
四、详细手动解题步骤（无任何代码）
 
1. 打开靶场链接，按下 F12 打开开发者工具，切换到 Network（网络） 面板。
2. 刷新页面，捕获到当前页面的访问数据包。
3. 鼠标右键该数据包，选择 Edit and resend（编辑并重发）。
4. 将顶部的 请求方法 GET 手动改成 POST。
5. 在请求体（Request body）位置，手动输入参数：
 what=flag 
6. 确认修改无误后，点击 Send（发送） 手动提交数据包。
7. 在服务器响应内容（Response）中，成功读取到 flag。
 
五、原理总结
 
1. GET 参数放在 URL 地址栏中，不安全、可见、长度有限。
2. POST 参数放在请求体中，地址栏不显示。
3. 本题限制只接收 POST 提交数据，因此必须手动修改请求方式。
4. 通过浏览器自带的网络抓包重发功能，可以纯手动构造合法 POST 数据包，无需任何脚本代码。
 
六、题目总结
 
这是一道非常经典的 HTTP 基础入门题。
主要让学习者理解：同一个网址，不同请求方式，服务器返回结果完全不同，掌握手动修改请求方法、手动传参的 Web 基础能力。
 
flag{bugku_post_987654321}
